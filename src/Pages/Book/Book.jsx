// import React, { use } from 'react';

const Book = ({ book }) => {
  // const bookData = use(booksPromise);
  // console.log(bookData);
  console.log(book);
  

  return <div></div>;
};

export default Book;